import { Delete } from './Delete';
import { Mode } from './Mode';
import { Toggle } from './Toggle';
import { Submit } from './Submit';

const Button = { Delete, Mode, Toggle, Submit };

export default Button;
